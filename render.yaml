services:
  - type: web
    name: clawmail-api
    runtime: node
    plan: free
    region: singapore
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: API_TOKEN
        generateValue: true
      - key: NODE_VERSION
        value: "20"
    healthCheckPath: /
